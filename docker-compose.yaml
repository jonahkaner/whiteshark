version: "3.9"

services:
  quicksand:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./keys:/app/keys:ro
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - KALSHI_API_KEY_ID=${KALSHI_API_KEY_ID:-}
